# Standards du Projet
- **Backend** : FastAPI ou Flask. Architecture en couches (Controller -> Service -> Repository).
- **Frontend** : React + TypeScript. Hooks personnalisés pour la logique, composants pour le rendu.
- **API** : Contrat strict. Toute modification API doit être reflétée dans les types TS du frontend.